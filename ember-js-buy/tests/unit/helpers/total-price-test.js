
import { totalPrice } from 'ember-js-buy/helpers/total-price';
import { module, test } from 'qunit';

module('Unit | Helper | total price');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = totalPrice([42]);
  assert.ok(result);
});

